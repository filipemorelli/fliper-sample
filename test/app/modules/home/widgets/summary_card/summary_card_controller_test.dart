import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:fliper/app/modules/home/widgets/summary_card/summary_card_controller.dart';
import 'package:fliper/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SummaryCardController summarycard;
  //
  setUp(() {
    //     summarycard = HomeModule.to.get<SummaryCardController>();
  });

  group('SummaryCardController Test', () {
    //   test("First Test", () {
    //     expect(summarycard, isInstanceOf<SummaryCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(summarycard.value, equals(0));
    //     summarycard.increment();
    //     expect(summarycard.value, equals(1));
    //   });
  });
}
